import theme, {COLORS, SIZES, FONTS, isAndroid, isIOS, GLOBAL_STYLE,INDICATOR} from "./theme"
import images from "./images"
import icons from "./icons"
// import font from "./fonts"

export {
    COLORS,
    SIZES,
    FONTS,
    isAndroid,
    GLOBAL_STYLE,
    INDICATOR,
    isIOS,
    theme,
    images,
    icons,

    // font
}